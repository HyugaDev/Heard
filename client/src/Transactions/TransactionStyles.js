import { createStyles, makeStyles } from "@mui/styles";

export const transactionStyles = makeStyles((theme) =>
  createStyles({
    heading: {
      textAlign: "center",
      padding: "10px"
    }
  })
);
